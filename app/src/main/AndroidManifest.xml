<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.noname.crenol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/loadinga"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/loadinga"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".vsRealPlayerActivity"></activity>
        <activity android:name=".AfterGameActivity"></activity>


        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Mainest2Activity"></activity>
        <activity android:name=".vsRealPlayerActivityEnternet"></activity>
        <activity android:name=".LoadingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>
    </application>

    </manifest>